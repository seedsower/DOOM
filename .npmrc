# Netlify-safe npm configuration
# Avoid /dev/null references that cause build issues

# Use default npm cache location
# cache=~/.npm

# Use default prefix
# prefix=/usr/local

# Disable audit warnings in CI
audit=false

# Set registry (optional)
registry=https://registry.npmjs.org/
